{
  "entrypoint": "${project.group}.${project.mainClass}",
  "name": "${project.name}",
  "version": "${project.version}",
  "dependencies": [
    "KStom"
  ],
  "meta": {
    "downloadURL": "https://github.com/Project-Cepi/Gooey/releases/download/latest/Gooey-1.0.0-all.jar"
  }
}